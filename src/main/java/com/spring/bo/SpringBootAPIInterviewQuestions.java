package com.spring.bo;

public class SpringBootAPIInterviewQuestions {
	
	/*   1. what is spring , why spring , how you use it ?
	 *   2. Maven why ?  what is use of it ? , how you will use it?
	 *   3. Spring beans , Spring Dependency Injection ? , Spring Inversion of control ?
	 *   4. Server what is server , tomcat ? , why tomcat ? spring boot uses embeded tomcat?
	 *   
	 * 
	 *  Project Strcuture -
	 *  
	 *  1. pox.xml why?
	 *  2. what are dependencies?
	 *  3. why dependencies ? how dependencies will be downloaded and used? 
	 *  4. where application will start ? it will starts from console application -.> main -> @SpringBootApplication
	 *  5. entry point is nothing but controller api url -> api end point ? HomeController -
	 *  
	 *  addDoctor()-> @PostMapping("addDoctor") -> @RequestMapping("api/home") -> localhost:8088/api/home/addDoctor (post)
	 *  
	 * 
	 * 1. what is api
	 * 2. what is api , JSON vs XML 
	 * 3. JSON or XMl which is used to sahre data between front end and backend is known as DTO (data transfer object)
	 * 4. DTO is binded using java class -> class -> name , id , desription ->obj.set, get ->JSON ()
	 * 
	 * 
	 * 1. @PostMapping ,@PutMapping , @DeleteMapping @GetMapping 
	 *   why , how , what is the purpose ?
	 *   
	 * 2. Relationships -> @OneToMany ,ManyToMany  ,ManyToOne , OneToOne - >why , how , what is the purpose 
	 * example ->  ManyToMany -> Filghts and Passengers 
	 * 
	 * 3. @Repository , @Component , @Autowired , @EnableJpaRepositories , @Service @ComponentScan
	 * @Setters @Getters 
	 * 
	 * 4. Http methods  -> GET , PUT ,POST , DELETE , PATCH , HEADERS
	 * 5. how can you login -> use Json WEb token (JWT token ) -. once you login we wil createa token for the logged in user
	 * and maintain session for him
	 * 
	 * so for the every time , when he is trying to login , he need to send token in HEADERS , JSON/xml
	 * 
	 * 200 , 400, 404, 500 , 409 , 201, 202 , 204 
	 * 404 , OK (200) , POST - created -201 , Accepeted -202 , 204 no content
	 * 
	 * 
	 * 
	 * 1. service layers , use of it , its annotationd @Service 
	 * 2. Data Access object dao layer , use of it , annotation @Repository
	 * 3. What is Hibernate ? 
	 * 4. What is Jpa 
	 * 5. Hibernate vs Jpa
	 * 
	 * 
	 * 
	 * 1. What is Cors -> cross origin resource sharing -> which URL or frontend you need to enable 
	 * @CrossOrigin -. on top of controller ->
	 *  
	 * 2. What is logging ? 
	 * 3. Beans scopes ? 
	 * Singleton, prototype, Transient
	 * 
	 * 4. why you implemented bo implementaion class using interface-?
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * */
}
